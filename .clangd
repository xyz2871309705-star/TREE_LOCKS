# TreeLocks — clangd 配置

CompileFlags:
  Add:
    - "-DTREELOCK_STATIC"
    - "-D_WIN32_WINNT=0x0600"
    - "-std=gnu11"

  Remove:
    - "-Wshadow"
    - "-Wconversion"

Diagnostics:
  # 抑制 clangd 与 MinGW 头文件不兼容导致的误报
  Suppress:
    - "*"
